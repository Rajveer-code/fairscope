{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"fairscope","text":"<p>Subgroup-stratified, calibration-aware fairness auditing for machine-learning models \u2014 grounded in peer-reviewed methods.</p> <p><code>fairscope</code> packages statistical machinery that mainstream fairness toolkits do not provide as first-class, subgroup-stratified functions:</p> <ul> <li>DeLong confidence intervals for per-subgroup AUC</li> <li>Expected Calibration Error per subgroup, with reliability diagrams</li> <li>Bootstrap significance testing of subgroup performance differences, with   multiple-comparison correction</li> <li>a subgroup-stratified interface to standard recalibration (temperature scaling,   isotonic regression)</li> <li>a novel five-axis Cross-Platform Fairness Evaluation (CPFE) protocol</li> <li>per-class fairness metrics (symmetric disparate impact, equalized odds difference)</li> </ul> <p>It ports established methods rather than inventing new ones; only the CPFE protocol is presented as novel, and every function cites its source. <code>fairscope</code> is for rigorous, uncertainty-aware measurement \u2014 it is complementary to mitigation-focused toolkits such as AIF360 and Fairlearn, not a replacement.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install fairscope\n</code></pre> <p>Heavy NLP dependencies (torch, transformers, captum) install via <code>pip install fairscope[nlp]</code>; SHAP via <code>pip install fairscope[shap]</code>. The base install stays light.</p>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Getting started \u2014 a real, runnable audit in a few lines.</li> <li>Healthcare audit \u2014 the one-call clinical fairness report.</li> <li>CPFE protocol \u2014 the five-axis cross-platform evaluation.</li> <li>API reference \u2014 every public function.</li> </ul> <p>Source and releases: github.com/Rajveer-code/fairscope \u00b7 MIT license.</p>"},{"location":"api/","title":"API reference","text":"<p>Auto-generated from the package docstrings.</p>"},{"location":"api/#top-level","title":"Top level","text":""},{"location":"api/#fairscope.FairnessAudit","title":"<code>fairscope.FairnessAudit(model, domain, **kwargs)</code>","text":"<p>Route to a domain-specific fairness audit.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>the fitted classifier (or ``None`` with precomputed scores via the domain API).</code> required <code>domain</code> <code>str</code> <p>The audit domain. Implemented: <code>\"healthcare\"</code> (uses <code>model</code>) and <code>\"nlp\"</code> (the CPFE protocol; operates on precomputed <code>platform_data</code>, <code>model</code> ignored).</p> required <code>**kwargs</code> <code>passed through to the domain audit class.</code> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fairscope\n&gt;&gt;&gt; callable(fairscope.FairnessAudit)\nTrue\n</code></pre>"},{"location":"api/#fairscopecore","title":"<code>fairscope.core</code>","text":""},{"location":"api/#fairscope.core","title":"<code>fairscope.core</code>","text":"<p>Core statistical primitives for fairscope.</p> <p>Public API for subgroup-stratified, calibration-aware fairness auditing: DeLong AUC confidence intervals and tests, a stratified bootstrap AUC test, calibration error and recalibration, multiple-comparison corrections, and subgroup fairness metrics.</p>"},{"location":"api/#fairscope.core.bootstrap_auc_test","title":"<code>bootstrap_auc_test(y_true, score_a, score_b, n_boot=2000, random_state=None)</code>","text":"<p>Stratified bootstrap test of (AUC_a - AUC_b) on the same samples.</p> <p>Resamples positives and negatives separately (preserving class balance). Returns a dict: auc_a, auc_b, delta, se, z, p_value, ci_lower, ci_upper, n_boot.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; rng = np.random.default_rng(0)\n&gt;&gt;&gt; y = rng.integers(0, 2, 400)\n&gt;&gt;&gt; a = rng.random(400) + 0.8 * y\n&gt;&gt;&gt; b = rng.random(400) + 0.05 * y\n&gt;&gt;&gt; bootstrap_auc_test(y, a, b, n_boot=200, random_state=0)[\"delta\"] &gt; 0\nTrue\n</code></pre>"},{"location":"api/#fairscope.core.ece_by_group","title":"<code>ece_by_group(y_true, y_prob, groups, n_bins=10, strategy='uniform')</code>","text":"<p>Per-subgroup Expected Calibration Error. Returns <code>{group: ece}</code>.</p>"},{"location":"api/#fairscope.core.expected_calibration_error","title":"<code>expected_calibration_error(y_true, y_prob, n_bins=10, strategy='uniform')</code>","text":"<p>Weighted mean |observed frequency - mean predicted probability| across bins.</p> <p>Returns 0.0 if there is no data. <code>strategy</code> is 'uniform' (equal-width) or 'quantile' (equal-frequency) bins.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; y = np.array([0, 0, 1, 1])\n&gt;&gt;&gt; p = np.array([0.0, 0.0, 1.0, 1.0])\n&gt;&gt;&gt; expected_calibration_error(y, p)\n0.0\n</code></pre>"},{"location":"api/#fairscope.core.isotonic_recalibrate","title":"<code>isotonic_recalibrate(probs, y_true)</code>","text":"<p>Fit isotonic regression mapping predicted prob -&gt; calibrated prob (Zadrozny &amp; Elkan, 2002). Returns <code>(fitted_model, calibrated_probabilities)</code>.</p>"},{"location":"api/#fairscope.core.maximum_calibration_error","title":"<code>maximum_calibration_error(y_true, y_prob, n_bins=10, strategy='uniform')</code>","text":"<p>Maximum |observed frequency - mean predicted probability| over bins.</p>"},{"location":"api/#fairscope.core.reliability_diagram","title":"<code>reliability_diagram(y_true, y_prob, groups=None, n_bins=10)</code>","text":"<p>Reliability diagram (mean predicted probability vs observed frequency).</p> <p>Returns a matplotlib <code>Figure</code>. If <code>groups</code> is given, draws one curve per subgroup.</p>"},{"location":"api/#fairscope.core.temperature_scale","title":"<code>temperature_scale(logits, y_true, max_iter=200)</code>","text":"<p>Fit a single temperature T&gt;0 by minimizing NLL (Guo et al., 2017).</p> <p><code>logits</code> may be 1-D (binary positive-class logit) or 2-D (n, n_classes). Returns <code>(T, calibrated_probabilities)</code>; calibrated probabilities are 1-D for 1-D input.</p>"},{"location":"api/#fairscope.core.benjamini_hochberg","title":"<code>benjamini_hochberg(p_values, alpha=0.05)</code>","text":"<p>Benjamini-Hochberg FDR correction (step-up). Returns adjusted, reject.</p>"},{"location":"api/#fairscope.core.bonferroni","title":"<code>bonferroni(p_values, alpha=0.05)</code>","text":"<p>Bonferroni correction. Returns adjusted, reject, threshold.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; bonferroni([0.01, 0.5], alpha=0.05)[\"adjusted\"].tolist()\n[0.02, 1.0]\n</code></pre>"},{"location":"api/#fairscope.core.delong_auc_ci","title":"<code>delong_auc_ci(y_true, y_score, alpha=0.05)</code>","text":"<p>AUC with a DeLong (1 - alpha) normal-approximation confidence interval.</p> <p>Returns a dict: auc, ci_lower, ci_upper, se, n_pos, n_neg.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; y = np.array([1, 1, 1, 1, 0, 0, 0, 0])\n&gt;&gt;&gt; s = np.array([0.9, 0.8, 0.7, 0.4, 0.6, 0.5, 0.3, 0.2])\n&gt;&gt;&gt; round(delong_auc_ci(y, s)[\"auc\"], 3)\n0.875\n</code></pre>"},{"location":"api/#fairscope.core.delong_by_group","title":"<code>delong_by_group(y_true, y_score, groups, alpha=0.05)</code>","text":"<p>Per-subgroup DeLong AUC CIs. Returns {group: ci_dict}.</p>"},{"location":"api/#fairscope.core.delong_paired_test","title":"<code>delong_paired_test(y_true, score_a, score_b)</code>","text":"<p>Covariance-aware paired DeLong test for two scores on the SAME samples.</p> <p>Returns: auc_a, auc_b, delta, z, p_value.</p>"},{"location":"api/#fairscope.core.delong_unpaired_test","title":"<code>delong_unpaired_test(y_true_a, score_a, y_true_b, score_b)</code>","text":"<p>Unpaired DeLong test for two INDEPENDENT samples (the cross-platform case).</p>"},{"location":"api/#fairscope.core.disparate_impact","title":"<code>disparate_impact(y_pred, groups, group_a, group_b)</code>","text":"<p>Symmetric disparate impact between two groups: min(rate_a/rate_b, rate_b/rate_a).</p> <p><code>rate = P(y_pred == 1 | group)</code> over hard 0/1 predictions. Result is in (0, 1]; &lt; 0.80 violates the four-fifths rule and &lt; 0.50 is a severe disparity (interpretation per the CPFE paper). If both rates are 0 the groups are treated as equal (returns 1.0); if exactly one rate is 0 the disparity is maximal (returns 0.0).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; y_pred = np.array([1, 1, 0, 0, 1, 0, 0, 0])\n&gt;&gt;&gt; g = np.array([\"A\", \"A\", \"A\", \"A\", \"B\", \"B\", \"B\", \"B\"])\n&gt;&gt;&gt; round(disparate_impact(y_pred, g, \"A\", \"B\"), 3)\n0.5\n</code></pre>"},{"location":"api/#fairscope.core.equalized_odds_difference","title":"<code>equalized_odds_difference(y_true, y_pred, groups, group_a, group_b)</code>","text":"<p>|TPR_a - TPR_b|, the equalized odds difference as defined in the CPFE paper.</p> <p><code>TPR = P(y_pred == 1 | y_true == 1, group)</code>. Raises if either group has no positive labels (TPR undefined).</p>"},{"location":"api/#fairscope.core.subgroup_metrics","title":"<code>subgroup_metrics(y_true, y_score, groups, threshold=0.5)</code>","text":"<p>Per-subgroup discrimination metrics: <code>{group: {auc, brier, f1, n}}</code>.</p> <p><code>y_score</code> are predicted probabilities in [0, 1] (Brier assumes a probability); <code>threshold</code> binarizes them for F1. Raises if any subgroup is single-class (AUC undefined) or if <code>y_score</code> contains NaN.</p>"},{"location":"api/#fairscopehealthcare","title":"<code>fairscope.healthcare</code>","text":""},{"location":"api/#fairscope.healthcare.audit","title":"<code>fairscope.healthcare.audit</code>","text":"<p>One-call clinical fairness audit. Composes fairscope.core; invents no statistics.</p> <p>Pipeline per protected attribute: per-subgroup DeLong AUC CIs -&gt; per-subgroup ECE -&gt; per-subgroup Brier/F1 -&gt; Bonferroni-corrected pairwise (unpaired) DeLong tests across the attribute's subgroups. Mirrors the analysis in the diabetes paper (IEEE CIPHER 2026).</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareFairnessAudit","title":"<code>HealthcareFairnessAudit</code>","text":"<p>Audit a fitted classifier (or precomputed scores) for subgroup fairness.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>object with ``predict_proba`` (positive-class probability in column 1), or</code> <p><code>None</code> when using :meth:<code>from_scores</code>.</p> required <code>X_test</code> <code>test features and binary labels.</code> required <code>y_test</code> <code>test features and binary labels.</code> required <code>protected_attr</code> <code>dict ``{attribute_name: 1-D array of subgroup labels per sample}``.</code> required"},{"location":"api/#fairscope.healthcare.audit.HealthcareFairnessAudit.from_scores","title":"<code>from_scores(y_true, y_score, protected_attr, *, n_bins=10, alpha=0.05)</code>  <code>classmethod</code>","text":"<p>Build an audit from precomputed positive-class probabilities (no model).</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareFairnessAudit.run","title":"<code>run()</code>","text":"<p>Run the audit and return a :class:<code>HealthcareReport</code>.</p> <p>Raises a clear, attribute-named <code>ValueError</code> if a subgroup is single-class (AUC undefined) rather than letting a low-level error surface.</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareFairnessAudit.shap_summary","title":"<code>shap_summary(max_samples=200)</code>","text":"<p>Mean absolute SHAP value per feature (optional). Requires <code>pip install fairscope[shap]</code> and a model (not <code>from_scores</code>). Returns a dict <code>{feature_index: mean_abs_shap}</code>.</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareReport","title":"<code>HealthcareReport</code>","text":"<p>Holds audit results and the raw (y, score, groups) needed to render reliability curves; provides tables (here), and plots/PDF (see plotting methods).</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareReport.summary","title":"<code>summary()</code>","text":"<p>Return a human-readable summary string (no print side effect).</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareReport.plot_auc_forest","title":"<code>plot_auc_forest(attribute=None)</code>","text":"<p>Forest plot of per-subgroup AUC with DeLong 95% CIs. Returns a Figure.</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareReport.plot_calibration","title":"<code>plot_calibration(attribute=None)</code>","text":"<p>Reliability curves per subgroup for one attribute, drawn with <code>core.reliability_diagram</code>. Returns a Figure.</p>"},{"location":"api/#fairscope.healthcare.audit.HealthcareReport.to_pdf","title":"<code>to_pdf(path)</code>","text":"<p>Write a multi-page PDF: summary, AUC forest, and one calibration page per attribute. Uses matplotlib only (no extra dependency).</p>"},{"location":"api/#fairscopenlp","title":"<code>fairscope.nlp</code>","text":""},{"location":"api/#fairscope.nlp","title":"<code>fairscope.nlp</code>","text":"<p>Cross-Platform Fairness Evaluation (CPFE) for NLP, built on fairscope.core.</p> <p>Public API: the five-axis <code>CPFEProtocol</code>/<code>CPFEReport</code>, the multiclass metric and significance primitives (axes 1-4), and the attribution-stability functions (axis 5).</p>"},{"location":"api/#fairscope.nlp.CPFEProtocol","title":"<code>CPFEProtocol</code>","text":"<p>Run the CPFE five-axis evaluation over precomputed per-platform outputs.</p> <p>Parameters:</p> Name Type Description Default <code>platform_data</code> <code>dict ``{name: {\"y_true\": array, \"probs\": (n, n_classes) array}}``.</code> required <code>reference</code> <code>the within-platform name (e.g. the training platform).</code> required <code>n_classes</code> <code>number of classes.</code> required <code>delta_auc_pct_max</code> <code>ILLUSTRATIVE macro-AUC-drop screening limit (percent) used by</code> <p><code>CPFEReport.deployment_readiness</code>. NOT a published cutoff: P4 Section 6.6 declines to set one (observed drops were 28.6-39.5%); the default echoes that \"&gt;30%\" magnitude and is labelled illustrative everywhere it surfaces.</p> <code>30.0</code>"},{"location":"api/#fairscope.nlp.CPFEReport","title":"<code>CPFEReport</code>","text":"<p>Holds the five-axis results and renders tables and a deployment-readiness diagnostic.</p>"},{"location":"api/#fairscope.nlp.CPFEReport.deployment_readiness","title":"<code>deployment_readiness()</code>","text":"<p>Structured per-axis, per-platform screening DIAGNOSTIC -- NOT a deployment decision. Following the CPFE paper (Sections 6.5-6.6), cross-platform degradation is an informative diagnostic, not definitive evidence of bias.</p> <p>Thresholds: calibration uses P4's stated ECE bands (Suppl. Fig. S2); equity uses P4's four-fifths rule; discrimination uses an ILLUSTRATIVE <code>delta_auc_pct_max</code> (P4 Section 6.6 declines to set a published cutoff). Returns <code>{platform: {\"ready\": bool, \"axes\": {axis: {pass, value, threshold, source, reason}}}}</code>.</p>"},{"location":"api/#fairscope.nlp.jaccard_topk","title":"<code>jaccard_topk(saliency_a, saliency_b, k)</code>","text":"<p>Jaccard overlap of the top-k tokens by saliency: <code>|topK(A) \u2229 topK(B)| / |union|</code>.</p> <p><code>saliency_*</code> map token -&gt; saliency score. Returns 0.0 if both are empty.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; jaccard_topk({\"a\": 0.9, \"b\": 0.8}, {\"a\": 0.7, \"c\": 0.6}, k=2)\n0.3333333333333333\n</code></pre>"},{"location":"api/#fairscope.nlp.token_saliency","title":"<code>token_saliency(model, tokenizer, text, target=None)</code>","text":"<p>Per-token gradient saliency <code>s_i = \u2016\u2202P(y|x)/\u2202E_i\u2016\u2082</code> via Captum (optional). Requires <code>pip install fairscope[nlp]</code>. Returns <code>{token: saliency}</code>.</p>"},{"location":"api/#fairscope.nlp.macro_auc","title":"<code>macro_auc(y_true, probs)</code>","text":"<p>Macro one-vs-rest AUC. Requires every class present in <code>y_true</code>.</p>"},{"location":"api/#fairscope.nlp.macro_f1","title":"<code>macro_f1(y_true, probs)</code>","text":"<p>Macro F1 of the argmax predictions.</p>"},{"location":"api/#fairscope.nlp.multiclass_ece","title":"<code>multiclass_ece(y_true, probs, n_bins=10)</code>","text":"<p>Confidence-accuracy Expected Calibration Error (Guo et al. 2017; CPFE paper): <code>ECE = sum_m (|B_m|/n) * |acc(B_m) - conf(B_m)|</code> with <code>conf = max prob</code> and <code>acc = top-1 correct</code>.</p>"},{"location":"api/#fairscope.nlp.per_class_disparate_impact","title":"<code>per_class_disparate_impact(probs_a, probs_b, n_classes)</code>","text":"<p>Symmetric DI per class between two platforms, reusing <code>core.disparate_impact</code> with the class binarized (<code>pred == c</code>) and platform as the two-group label.</p>"},{"location":"api/#fairscope.nlp.per_class_equalized_odds","title":"<code>per_class_equalized_odds(y_a, probs_a, y_b, probs_b, n_classes)</code>","text":"<p>EOD per class between two platforms (<code>|TPR_c(A) - TPR_c(B)|</code>), reusing <code>core.equalized_odds_difference</code>. A class with no positive labels in a platform is returned as <code>None</code> (TPR undefined).</p>"},{"location":"api/#fairscope.nlp.bootstrap_macro_auc_test","title":"<code>bootstrap_macro_auc_test(y_a, probs_a, y_b, probs_b, n_boot=2000, random_state=None)</code>","text":"<p>Compare macro AUC across two platforms (independent test sets).</p> <p>Each platform's macro-AUC standard error is estimated by a class-stratified bootstrap; the errors are combined for an unpaired z-test. Returns a dict: auc_a, auc_b, delta, se, z, p_value, n_boot.</p>"},{"location":"cpfe/","title":"Cross-Platform Fairness Evaluation (CPFE)","text":"<p>CPFE is <code>fairscope</code>'s novel contribution: a five-axis protocol for evaluating what happens when a model trained on one data source is deployed on another. A single AUC number cannot distinguish the ways a model degrades across platforms; CPFE evaluates five orthogonal axes, comparing each non-training platform against a reference.</p>"},{"location":"cpfe/#the-five-axes","title":"The five axes","text":"<ol> <li>Discriminative performance \u2014 macro one-vs-rest AUC and macro F1, with the relative    change \u0394AUC%.</li> <li>Calibration \u2014 confidence\u2013accuracy Expected Calibration Error per platform.</li> <li>Statistical significance \u2014 a stratified bootstrap standard error on the macro-AUC    difference between platforms (independent test sets), with Bonferroni correction.</li> <li>Prediction equity \u2014 symmetric disparate impact and equalized-odds difference per class,    treating platform membership as the group.</li> <li>Attribution stability \u2014 Jaccard overlap of the top-K gradient-saliency token sets    across platforms (requires <code>pip install fairscope[nlp]</code>).</li> </ol>"},{"location":"cpfe/#usage","title":"Usage","text":"<p>The protocol runs on precomputed per-platform outputs (axes 1\u20134 need no deep-learning dependencies):</p> <pre><code>from fairscope.nlp import CPFEProtocol\n\nplatform_data = {\n    \"reference\": {\"y_true\": y_ref, \"probs\": probs_ref},   # (n, n_classes)\n    \"deployment\": {\"y_true\": y_dep, \"probs\": probs_dep},\n}\nreport = CPFEProtocol(platform_data, reference=\"reference\", n_classes=4).run()\n\nreport.to_dataframe()          # macro AUC/F1, ECE, \u0394AUC% per platform\nreport.significance            # bootstrap macro-AUC test + Bonferroni-adjusted p\nreport.equity                  # per-class disparate impact and equalized odds\nreport.deployment_readiness()  # structured per-axis diagnostic\n</code></pre>"},{"location":"cpfe/#deployment_readiness-a-diagnostic-not-a-decision","title":"<code>deployment_readiness()</code> \u2014 a diagnostic, not a decision","text":"<p><code>deployment_readiness()</code> returns a structured per-axis verdict (<code>{platform: {ready, axes: {axis: {pass, value, threshold, source, reason}}}}</code>). It is explicitly a screening diagnostic, not a deployment decision or compliance verdict.</p> <p>Each axis reports the threshold it used and that threshold's source:</p> <ul> <li>Equity uses the four-fifths rule (disparate impact &lt; 0.80 is a violation, &lt; 0.50 severe)   \u2014 a stated reference value.</li> <li>Calibration uses stated reference bands (ECE &lt; 0.10 well-calibrated, &gt; 0.20 moderate   miscalibration).</li> <li>Discrimination uses an illustrative, user-configurable \u0394AUC limit   (<code>delta_auc_pct_max</code>) \u2014 not a published decision threshold; the underlying study   explicitly declines to set one.</li> </ul> <p>Cross-platform degradation is an informative signal that warrants platform-specific validation; it is not, by itself, evidence of algorithmic bias.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install fairscope\n</code></pre>"},{"location":"getting-started/#a-real-audit-in-a-few-lines","title":"A real audit in a few lines","text":"<p>The example below is actually runnable \u2014 it uses the synthetic fairness fixture committed to the repository. Clone the repo and run it from the repository root:</p> <pre><code>git clone https://github.com/Rajveer-code/fairscope\ncd fairscope\npip install -e .\n</code></pre> <pre><code>import pandas as pd\n\nfrom fairscope.healthcare import HealthcareFairnessAudit\n\n# A SYNTHETIC fixture committed in the repo (NOT real BRFSS data). It is seed-generated to\n# reproduce the direction and approximate magnitude of a published age-disparity finding\n# (elderly &lt; young AUC). See tests/fixtures/README.md.\ndf = pd.read_csv(\"tests/fixtures/healthcare_subsample.csv\", comment=\"#\")\n\nreport = HealthcareFairnessAudit.from_scores(\n    df[\"y_true\"].to_numpy(),\n    df[\"y_score\"].to_numpy(),\n    {\"age_group\": df[\"age_group\"].to_numpy()},\n).run()\n\n# Per-subgroup AUC with DeLong CIs, ECE, Brier, F1:\nprint(report.to_dataframe())\n\n# A readable summary that flags the largest gap and any Bonferroni-significant difference:\nprint(report.summary())\n</code></pre> <p>Running this prints a per-subgroup table and a summary reporting that the elderly group has a substantially lower AUC than the young group, with the gap flagged as statistically significant after Bonferroni correction \u2014 the audit recovering the published finding's direction on the synthetic fixture.</p> <p>Note</p> <p>The fixture is synthetic and small; it demonstrates the audit pipeline. It does not reproduce the full published run (which used a cohort of ~1.28M records). See <code>tests/fixtures/README.md</code>.</p>"},{"location":"getting-started/#using-your-own-model","title":"Using your own model","text":"<p>If you have a fitted scikit-learn-style classifier with <code>predict_proba</code>, pass it directly:</p> <pre><code>from fairscope import FairnessAudit\n\nreport = FairnessAudit(\n    model, domain=\"healthcare\", X_test=X_test, y_test=y_test,\n    protected_attr={\"age_group\": age_groups, \"sex\": sex},\n).run()\n</code></pre> <p>For cross-platform NLP evaluation, see the CPFE protocol.</p>"},{"location":"healthcare/","title":"Healthcare audit","text":"<p><code>HealthcareFairnessAudit</code> is a one-call clinical fairness audit. It composes the <code>core</code> primitives \u2014 it invents no statistics \u2014 into a per-subgroup report for each protected attribute.</p>"},{"location":"healthcare/#what-it-computes","title":"What it computes","text":"<p>For each protected attribute, and each subgroup within it:</p> <ul> <li>per-subgroup AUC with a DeLong confidence interval</li> <li>per-subgroup Expected Calibration Error</li> <li>per-subgroup Brier score and F1</li> <li>Bonferroni-corrected pairwise (unpaired) DeLong tests across the attribute's subgroups   (subgroups are disjoint samples, so the comparison is unpaired)</li> </ul>"},{"location":"healthcare/#two-entry-points","title":"Two entry points","text":"<pre><code>from fairscope.healthcare import HealthcareFairnessAudit\n\n# 1) From a fitted model with predict_proba:\nreport = HealthcareFairnessAudit(\n    model, X_test, y_test, protected_attr={\"age_group\": ages, \"sex\": sex}\n).run()\n\n# 2) From precomputed positive-class probabilities (no model needed):\nreport = HealthcareFairnessAudit.from_scores(\n    y_true, y_score, protected_attr={\"age_group\": ages}\n).run()\n</code></pre>"},{"location":"healthcare/#the-report","title":"The report","text":"<pre><code>report.to_dataframe()      # tidy per-subgroup table\nreport.summary()           # text summary; flags the largest gap and significant differences\nreport.plot_auc_forest()   # forest plot of per-subgroup AUC with DeLong CIs\nreport.plot_calibration()  # reliability curves per subgroup\nreport.to_pdf(\"report.pdf\")  # multi-page PDF (matplotlib only)\n</code></pre> <p>An optional SHAP feature summary is available via <code>audit.shap_summary()</code> when the <code>fairscope[shap]</code> extra is installed and a model is provided.</p> <p>See Getting started for a runnable example on the committed fixture.</p>"}]}